import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  
  // Primary (Trust, Professionalism)
  static const Color primary = Color(0xFF4F46E5); // Modern Indigo
  static const Color primaryLight = Color(0xFF818CF8);
  static const Color primaryDark = Color(0xFF3730A3);

  // Secondary/Accent (Speed, Practicality)
  static const Color secondary = Color(0xFF10B981); // Emerald Green
  
  // Background & Surfaces
  static const Color background = Color(0xFFF9FAFB); // Very light grey, almost white
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFEF4444);
  
  // Text Colors
  static const Color textPrimary = Color(0xFF111827); // Dark grey
  static const Color textSecondary = Color(0xFF6B7280); // Muted grey
  static const Color textHint = Color(0xFF9CA3AF);
  
  // Borders & Dividers
  static const Color border = Color(0xFFE5E7EB);
  
  // Utility
  static const Color iconColor = Color(0xFF4B5563);
}
